sudo: required

services:
  - docker

before_install:
  - docker build -t ldboo/point_news_ai .

script:
  - docker run ldboo/point_news_ai npm test

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

  - docker push ldboo/point_news_ai
